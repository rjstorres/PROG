#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <cctype>
#include <algorithm>

using namespace std;
int leInteiro(int min);
bool menu_input_error(unsigned short int &toinput, const int maxop);
string strtrim(string str);
string readstring();


